fn maybe_square(base: u16, square: bool) -> u16 {
    if (square) {
        base * base
    } else {
        base
    }
}

fn main() {
    dbg!(maybe_square(16, false));
    dbg!(maybe_square(16, true));
    let limit = 0 in
    let x = 10 in
    for x downto (limit) {
        dbg!(x);
    }
}